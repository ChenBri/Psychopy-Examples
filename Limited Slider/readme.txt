This experiment demonstrates a method where the participant can use a slider only when a particular value inside a range is being chosen.

The slider only shows once pressed at a particular point on the range.